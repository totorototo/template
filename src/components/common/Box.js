import styled from "styled-components";
import {
  space,
  layout,
  typography,
  color,
  background,
  border,
  position,
  shadow,
  gridArea
} from "styled-system";

const Box = styled.div`
  ${space}
  ${layout}
  ${typography}
  ${color}
  ${background}
  ${border}
  ${position}
  ${shadow}
  ${gridArea}
`;

export default Box;
